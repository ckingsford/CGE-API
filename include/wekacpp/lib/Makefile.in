#
# @COPYRIGHT
# @
#
# $Id$
#

include @abs_top_builddir@/Makefile.config

SrcDir = @srcdir@
ObjDir = @builddir@

SUBDIRS  =

ARCHIVE = libWeka++
SOLIB = libWeka++-1.0

include @abs_top_srcdir@/Makefile.rules

install::
	cd $(libdir) && ln -sf libWeka++-1.0.so libWeka++.so

instclean::
	cd $(libdir) && rm -f libWeka++*
